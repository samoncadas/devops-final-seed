version: '3.8'

services:
  todo-api:
    build: .
    container_name: todo_api_container
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DB_PATH=tasks.db
    volumes:
      - ./data:/app/data
    restart: always